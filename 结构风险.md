# 结构风险、经验风险与正则化

## 1. 经验风险是什么

在监督学习中，给定训练集：

$$
\mathcal{D}=\{(x_i,y_i)\}_{i=1}^n
$$

模型记为：

$$
\hat y = f(x;\theta)
$$

其中：

- $x_i$ 是输入
- $y_i$ 是标签
- $\theta$ 是模型参数
- $\ell(f(x_i;\theta), y_i)$ 是单个样本的损失

那么，**经验风险**（empirical risk）就是训练集上的平均损失：

$$
L_{emp}(\theta)=\frac{1}{n}\sum_{i=1}^n \ell(f(x_i;\theta),y_i)
$$

经验风险衡量的是：

> 模型在已经看到的训练样本上，预测错得多不多。

---

## 2. 经验风险最小化的问题

如果训练时只追求经验风险最小，也就是只想让：

$$
L_{emp}(\theta)
$$

尽可能小，那么模型可能会把训练数据拟合得非常好，甚至记住训练数据中的噪声。

这会带来一个问题：

- 训练集误差很小
- 测试集误差却很大

这就是**过拟合**。

所以只看经验风险，往往不够。

---

## 3. 结构风险是什么

为了避免模型一味追求拟合训练集，我们通常不仅考虑训练误差，还考虑模型复杂度。

这就得到**结构风险**（structural risk）：

$$
L_{struct}(\theta)=L_{emp}(\theta)+\lambda \Omega(\theta)
$$

其中：

- $L_{emp}(\theta)$：经验风险
- $\Omega(\theta)$：复杂度惩罚项，也叫正则项
- $\lambda$：控制正则化强度的系数

结构风险的含义是：

> 模型不仅要在训练集上表现好，还不能过于复杂。

---

## 4. 为什么叫“结构风险”

这里的“结构”不是单指网络层数，而是更广义的模型复杂度。

直观上：

- 简单模型：表达能力有限，但不容易过拟合
- 复杂模型：表达能力强，但更容易把噪声也学进去

因此，结构风险最小化就是在下面两者之间找平衡：

- 拟合训练数据
- 保持模型简单

---

## 5. 结构风险最小化的常见形式

实际训练时，通常优化的是下面这个目标函数：

$$
J(\theta)=L_{emp}(\theta)+\lambda \Omega(\theta)
$$

这就是结构风险最小化最常见的可计算形式。

也就是说，训练不是只最小化训练误差，而是最小化：

- 训练误差
- 加上复杂度代价

---

## 6. 正则项在计算什么

正则项 $\Omega(\theta)$ 本质上是在度量，正则项衡量数值复杂度：

- 参数是否过大
- 参数是否过于极端
- 模型是否过于复杂
- 模型函数是否过于敏感

所以正则项不一定总是“直接算结构”，但通常是在**固定模型结构下，限制参数的复杂程度**。

---

## 7. 常见正则化方案

### 7.1 L2 正则

最常见的是 $L_2$ 正则：

$$
\Omega(\theta)=\|\theta\|_2^2=\sum_j \theta_j^2
$$

对应目标函数：

$$
J(\theta)=L_{emp}(\theta)+\lambda \sum_j \theta_j^2
$$

它的作用是：

- 惩罚过大的参数
- 让参数整体变小
- 通常不会把参数直接压到 0

---

### 7.2 L1 正则

$L_1$ 正则写成：

$$
\Omega(\theta)=\|\theta\|_1=\sum_j |\theta_j|
$$

对应目标函数：

$$
J(\theta)=L_{emp}(\theta)+\lambda \sum_j |\theta_j|
$$

它的作用是：

- 惩罚大参数
- 更容易把部分参数压成 0
- 常用于稀疏建模和特征选择

---

### 7.3 Elastic Net

Elastic Net 是 $L_1$ 和 $L_2$ 的组合：

$$
\Omega(\theta)=\alpha \|\theta\|_1+\beta \|\theta\|_2^2
$$

目标函数为：

$$
J(\theta)=L_{emp}(\theta)+\lambda\left(\alpha \|\theta\|_1+\beta \|\theta\|_2^2\right)
$$

它同时兼顾：

- 稀疏性
- 稳定性

---

### 7.4 其他常见正则化方法

除了把正则项显式写进损失函数，还常见下面这些隐式正则化方法：

- Dropout
- Early stopping
- Weight decay
- 数据增强
- 剪枝（pruning）

这些方法虽然不一定都写成 $\Omega(\theta)$，但本质上也都在帮助控制模型复杂度、提高泛化能力。

---

## 8. 为什么“正则项小”会让模型更简单

这里要区分两种复杂度：

### 8.1 结构复杂度

例如：

- 网络有几层
- 每层有多少神经元
- 是不是 Transformer
- 是不是决策树

这些属于模型的**架构**。

### 8.2 参数复杂度

例如：

- 参数值是否很大
- 模型输出对输入是否过于敏感
- 学出来的函数是否很扭曲、很陡峭

正则项通常主要控制的是第二种复杂度。

也就是说：

> 正则化通常不直接改变模型结构，而是改变同一结构下学出来的函数形状。

---

## 9. 为什么参数变小，函数会更简单

看最简单的线性模型：

$$
f(x)=wx+b
$$

它的导数是：

$$
\frac{df}{dx}=w
$$

所以：

- 如果 $w$ 很大，函数变化很剧烈
- 如果 $w$ 很小，函数变化更平缓

这说明参数大小直接影响函数对输入的敏感程度。

在高维线性模型中：

$$
f(\mathbf{x})=\mathbf{w}^T\mathbf{x}+b
$$

若输入变化为：

$$
\Delta \mathbf{x}
$$

则输出变化满足：

$$
\Delta f=\mathbf{w}^T \Delta \mathbf{x}
$$

并且有上界：

$$
|\Delta f|\le \|\mathbf{w}\|\cdot \|\Delta \mathbf{x}\|
$$

所以：

- 权重范数越大，模型对输入越敏感
- 权重范数越小，模型通常越平滑

因此，正则化虽然没改结构，但会让学出来的函数更“温和”。

---

## 10. 结构不变，为什么还说模型更简单

因为这里说的“简单”，通常不是指：

- 层数变少
- 节点数减少
- 网络结构改变

而是指：

- 参数没那么极端
- 输出没那么敏感
- 决策边界没那么扭曲
- 模型更不容易贴着噪声走

所以更准确地说：

> 正则化不一定让模型结构更简单，但会让模型学到的函数更简单、更平滑、更容易泛化。

---

## 11. L1 正则为什么还会让“有效结构”变简单

对于 $L_1$ 正则：

$$
\Omega(\theta)=\sum_j |\theta_j|
$$

训练后常常会出现某些参数被压成 0。

例如：

$$
f(\mathbf{x})=w_1x_1+w_2x_2+w_3x_3+b
$$

如果训练后得到：

$$
w_2=0,\quad w_3=0
$$

那么模型实际上只在使用 $x_1$。

所以虽然“名义上的结构”没变，但“有效使用的连接/特征”变少了。

这也是为什么 $L_1$ 正则常常会带来稀疏性。

---

## 12. 正常训练时一般优化哪个风险

通常训练时代码里真正优化的是一个目标函数：

$$
J(\theta)=L_{emp}(\theta)+\lambda \Omega(\theta)
$$

因此从实践角度说：

- 如果没有正则项，主要是在做经验风险最小化
- 如果有正则项，通常是在做结构风险最小化的一个具体实现

也就是说，现代训练中很常见的做法是：

> 优化“经验风险 + 正则项”

---

## 13. 如何做到结构风险最小化

最常见的方法包括：

- 在损失函数中加入 $L_1$ 或 $L_2$ 正则项
- 通过 weight decay 抑制参数过大
- 用 dropout 减少过度依赖
- 用 early stopping 防止训练过久
- 用验证集调节正则化强度 $\lambda$
- 控制模型规模，例如层数、宽度、树深度

这些做法本质上都在实现同一件事：

> 不让模型为了降低训练误差而变得过于复杂。

---

## 14. 有模型会直接调整结构吗

大多数常见神经网络训练时，结构是固定的，训练中优化的是参数：

$$
\theta^*=\arg\min_{\theta} L(\theta)
$$

但也有一些方法会直接或间接调整结构，例如：

- 决策树训练：一边训练一边长出树结构
- 剪枝：训练中或训练后删掉连接、通道、神经元
- 稀疏学习：让某些连接失效
- 神经架构搜索（NAS）：把结构也作为搜索对象
- Boosting：训练过程中逐步增加新的基学习器

所以：

- 普通深度学习：通常固定结构，只学参数
- 某些特定方法：会直接或间接改结构

---

## 15. 一句话总结

### 经验风险

只看训练集上的平均误差：

$$
L_{emp}(\theta)=\frac{1}{n}\sum_{i=1}^n \ell(f(x_i;\theta),y_i)
$$

### 结构风险

在经验风险基础上，再加复杂度惩罚：

$$
L_{struct}(\theta)=L_{emp}(\theta)+\lambda \Omega(\theta)
$$

### 核心理解

- 经验风险关注：训练数据上错多少
- 结构风险关注：训练数据上错多少，同时模型是否过复杂
- 正则化通常不直接改模型结构，而是让固定结构下学到的函数更简单、更平滑、更不容易过拟合

---

## 16. 最短版速记

- **经验风险**：训练误差
- **结构风险**：训练误差 + 复杂度惩罚
- **正则项**：给复杂模型加代价
- **L2 正则**：让参数整体变小
- **L1 正则**：让参数变稀疏
- **正则化不一定改结构，但会改变学出来的函数形状**